const SectionTitle = ({ title }) => {
    return (
        <div className="row">
            <h2 className="mt-5 mb-3 col-7"><strong>{title}</strong></h2>
        </div>
    )
}

export default SectionTitle;